'use strict';

console.log('--- loading: _.js');

const addTaskHandler = () => {
    debugger;

    const userInput = prompt("Add a new task: ");
    if (userInput === null) {
        return;
    }

    

};

